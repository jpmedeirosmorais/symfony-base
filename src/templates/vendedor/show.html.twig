{% extends 'base.html.twig' %}

{% block title %}Vendedor{% endblock %}

{% block body %}
    <h1>Vendedor</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ vendedor.id }}</td>
            </tr>
            <tr>
                <th>Nome</th>
                <td>{{ vendedor.nome }}</td>
            </tr>
            <tr>
                <th>Cpf</th>
                <td>{{ vendedor.cpf }}</td>
            </tr>
            <tr>
                <th>Nascimento</th>
                <td>{{ vendedor.nascimento ? vendedor.nascimento|date('d-m-Y') : '' }}</td>
            </tr>
            <tr>
                <th>Comissao</th>
                <td>{{ vendedor.comissao }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('vendedor_index') }}">back to list</a>

    <a href="{{ path('vendedor_edit', {'id': vendedor.id}) }}">Editar</a>

    {{ include('vendedor/_delete_form.html.twig') }}
{% endblock %}
