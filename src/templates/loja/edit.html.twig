{% extends 'base.html.twig' %}

{% block title %}Edit Loja{% endblock %}

{% block body %}
    <h1>Editar Loja</h1>

    {{ include('loja/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('loja_index') }}">Voltar a lista</a>

    {{ include('loja/_delete_form.html.twig') }}
{% endblock %}
