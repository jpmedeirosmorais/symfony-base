<form method="post" action="{{ path('loja_delete', {'id': loja.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ loja.id) }}">
    <button class="btn">Delete</button>
</form>
